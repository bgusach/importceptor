============
Installation
============

At the command line::

    $ easy_install importceptor

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv importceptor
    $ pip install importceptor